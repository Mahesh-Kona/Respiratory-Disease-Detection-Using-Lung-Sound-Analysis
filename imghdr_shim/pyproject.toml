[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "imghdr"
version = "0.1.0"
description = "Stdlib-compatible imghdr shim for Python 3.13+"
requires-python = ">=3.13"
